# EMG フォーマット 仕様書 v0.4.0（Draft）

**バージョン:** 0.4.0（Draft）
**更新日:** 2026-08-24
**基準:** `emg-json-spec.md`（v0.3.0）

---

## 0. この文書について

### 0.1 差分仕様である

本書は **v0.3.0 からの差分のみ**を定義します。ここに記述のない事項は `emg-json-spec.md`（v0.3.0）がそのまま適用されます。v0.3.0 の記述と本書が矛盾する場合は本書が優先します。

### 0.2 v0.4.0 の目的

**v0.4.0 は表現力を増やしません。** アバターの見た目に関して v0.3.0 でできなかったことは、v0.4.0 でもできません。

目的は、以後のバージョンで機能を追加しても既存の実装とファイルが壊れないようにする**土台を用意すること**です。具体的には次の 3 つです。

1. 未知のフィールド・未知の値に遭遇した実装の振る舞いを定める（1 章）
2. 「読めないと表示が成立しない追加」を宣言する手段を用意する（2 章）
3. v0.5.0 で必要になるが、それ自体は描画に影響しないフィールドを先に確定させる（3 章）

### 0.3 なぜ先に行う必要があるか

**前方互換の規定は、対応した実装が配布される前にしか導入できません。**

規定を後から追加しても、すでに配布済みの実装の振る舞いは変わりません。救われるのは規定に対応した実装だけです。したがって v0.4.0 は、**実装が広く配布される前**に完了している必要があります。

### 0.4 v0.3.0 ファイルとの関係

**v0.3.0 のファイルはすべて、変更なしに v0.4.0 のファイルとして有効です。** 本書が追加するフィールドはすべて任意であり、不在時の意味が v0.3.0 の挙動と一致するよう定義されています（1.3）。

`version` の値は `"0.4.0"` となりますが、**実装は `version` を解釈の分岐に使ってはなりません**（v0.3.0 仕様 2.1）。

---

## 1. 互換性の規則（規範的・新規）

v0.3.0 にはこれに相当する規定が一切ありませんでした。

### 1.1 後方互換 — 新しい実装が古いファイルを読む

適合実装は、v0.3.0 以降のすべてのファイルを追加の指定なしに読めなければなりません。

仕様の側は、以後のバージョンで次を守ります。

| # | 規則 |
|---|---|
| B1 | `version` を解釈の分岐に使わない。構造の判定はフィールドの有無で行う |
| B2 | 既存キーの意味を変更しない。異なる意味で再利用もしない。意味を変える必要が生じた場合は新しいキー名を用いる |
| B3 | 任意のフィールドを必須へ変更しない |
| B4 | 新規フィールドには必ず「不在時の意味」を定義し、1.3 の表に記載する |
| B5 | 読み込みの対応は維持する。引退させるのは書き出し側のみ |

### 1.2 前方互換 — 古い実装が新しいファイルを読む

適合実装は次に従わなければなりません。

| # | 規則 |
|---|---|
| **F1** | **未知のルートキー、および未知のオブジェクトフィールドは無視して読み進めなければならない。** 存在を理由にエラーとしてはならない |
| **F2** | `parts[].type` が既知の値（`static` / `switch`）でない場合、**`default` フィールドを持つなら `switch`、持たないなら `static`** として扱わなければならない |
| **F3** | `sequence.type` が未知の値の場合、`ordered` として扱わなければならない |
| **F4** | `trigger.type` が未知の値の場合、**自律発火を行ってはならない**（`external` と同じ扱いとする） |
| **F5** | `requiredExtensions[]` に未知の識別子が含まれる場合、読み込みを拒否しなければならない（2 章） |

#### F2 の根拠

r2 方針書は「未知の `type` は `static` として扱う」を案としていましたが、本書は `default` の有無で判定します。B1（フィールドの有無で判定する）と一貫し、劣化の仕方が用途に応じて妥当になるためです。

| 将来追加されうる種別 | `default` | F2 の判定 | 劣化の見え方 |
|---|---|---|---|
| 独立トグル（眼鏡・帽子） | 持たない | `static` | 常に表示される。外せないだけで絵は成立する |
| 排他的な新種別 | 持つ | `switch` | 1 枚だけ表示される。全レイヤーが重なるより破綻が小さい |

#### F1 と F5 の関係

F1（未知のものは無視）と F5（未知のものは拒否）は一見矛盾しますが、対象が異なります。

- **F1**: 実装が知らなくても表示が成立する追加 → 無視して読み進める
- **F5**: 実装が知らないと表示が成立しない追加 → 明示的に失敗する

沈黙して誤った絵を出すよりも、検知できる状態を優先します。

### 1.3 不在時の意味（規範的）

新規フィールドを追加する際は、この表に 1 行を追加しなければなりません（B4）。「v0.3.0 の挙動と一致」がすべて ○ である限り、0.4 節の互換性は定義上保たれます。

| フィールド | 不在時の解釈 | v0.3.0 の挙動と一致 |
|---|---|---|
| `requiredExtensions[]`（ルート） | 空配列 | ○ |
| `layers[].anchor_x` | `basePosition_x` と同値 | ○ |
| `layers[].anchor_y` | `basePosition_y` と同値 | ○ |
| `sprites[].fps` | `sequence.frames` を用いる場合は `12` | ○（v0.3.0 では必須のため不在にならない） |
| `layers[].opacity` | `1.0` | ○（v0.3.0 で規定済み） |
| `layers[].blendMode` | `"normal"` | ○（v0.3.0 で規定済み） |

### 1.4 正規化レイヤー（参考）

実装は、読み込み直後に既定値を補完し、以降のコードが単一の形式のみを扱う構成にすることを推奨します。

```
parse(json) → 既定値を補完 → 最新形の内部モデル → 描画・UI（バージョン分岐なし）
```

バージョン差の吸収がこの一箇所に閉じ、描画や UI にバージョン分岐が漏れません。

---

## 2. `requiredExtensions[]`（規範的・新規）

### 2.1 定義

ルートオブジェクトに任意フィールドとして追加します。

```json
{
  "version": "0.4.0",
  "requiredExtensions": ["EMG_frame_name"],
  "baseCanvasWidth": 2894,
  ...
}
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `requiredExtensions` | string[] | ❌ | このファイルを正しく表示するために実装が理解している必要がある**機能識別子**の配列。不在時は空配列 |

### 2.2 意味論

**要素は「拡張パッケージ名」ではなく「機能識別子」です。** コア仕様に追加されたフィールドであっても、それを理解しない実装が誤った絵を出す場合はここで宣言します。

読み込み実装は次に従わなければなりません。

1. `requiredExtensions[]` の各要素が、自身の理解する識別子の集合に含まれるか確認する
2. 1 つでも含まれないものがあれば、**読み込みを拒否し、理由として当該識別子を提示しなければならない**
3. すべて含まれる場合のみ、通常どおり読み込む

### 2.3 識別子

識別子は **`emg-extensions-registry.md`（機能識別子 登録簿）** で一元管理します。識別子はバージョンをまたいで追加されるため、各バージョンの仕様書ではなく単一の文書に集約します。命名は `EMG_` を接頭辞とし、小文字のスネークケースを続けます。

登録簿に載っていない識別子は、「理解しない識別子」として 2.2 の手順で扱わなければなりません。

**v0.4.0 が定義する識別子はありません。** v0.4.0 で追加されるフィールドはいずれも、理解しない実装が無視しても表示が成立するためです（1.3 ですべて ○）。

本フィールドは v0.5.0 以降のための仕組みとして先に導入します。

> **書き出し側への注意:** 理解しなくても表示が成立する追加に対して `requiredExtensions` を宣言してはなりません。不必要に古い実装を締め出すことになります。

---

## 3. `layers[].anchor_x` / `anchor_y`（規範的・新規）

### 3.1 定義

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `anchor_x` | number | ❌ | 回転・拡縮の中心の X 座標。**キャンバス座標**。不在時は `basePosition_x` |
| `anchor_y` | number | ❌ | 回転・拡縮の中心の Y 座標。**キャンバス座標**。不在時は `basePosition_y` |

```json
{
  "textureID": "hair_back",
  "x": 0, "y": 0, "width": 200, "height": 400,
  "basePosition_x": 600, "basePosition_y": 300,
  "anchor_x": 700, "anchor_y": 320,
  "textureZIndex": 12
}
```

### 3.2 v0.4.0 における効果

**v0.4.0 に座標変換のアニメーションは存在しないため、この 2 つのフィールドは描画に影響しません。**

適合実装は、値を読み取り内部モデルに保持しなければなりませんが、v0.4.0 の範囲では使用しません。v0.5.0 でトランスフォームが導入された際に、実装の変更が読み込み側へ波及しないようにするための先行定義です。

### 3.3 座標系を「キャンバス絶対」とする理由

レイヤー矩形からの相対値ではなく、`basePosition` と同じキャンバス座標系の絶対値とします。

アトラスの再パッキングやトリム範囲の変更が起きても、**アンカーが指す解剖学的な位置は変わらない**ためです。相対値にすると、トリムが 1px 変わるたびにアンカーの振り直しが必要になります。

不在時の既定値を `basePosition` と同値にすることで、v0.3.0 のファイルは「レイヤー矩形の左上を中心とする」と解釈され、既存の描画結果と一致します（v0.4.0 では回転が存在しないため、いずれにせよ観測可能な差はありません）。

---

## 4. `sprites[].fps` の任意化（規範的・変更）

### 4.1 変更内容

v0.3.0 で**必須**であった `sprites[].fps` を**任意**に変更します。

これは必須 → 任意の緩和であり、既存ファイルを無効にしません（B3 は任意 → 必須を禁じるものであり、その逆は許容されます）。

### 4.2 不在時の扱い

`sequence` が `frames` を用いる場合、`fps` が不在なら **`12`** として扱わなければなりません。

v0.5.0 で導入が検討されている不等間隔のキー指定（`sequence.keys`）では `fps` は不要になります。本変更はそのための準備です。

> **書き出し側への注意:** v0.4.0 の時点では、`fps` を省略する理由はありません。従来どおり明示してください。

---

## 5. `blendMode` の許容値（規範的・確定）

v0.3.0 は `blendMode` の値の集合を規定していませんでした（v0.3.0 仕様 10.1）。v0.4.0 で確定させます。

### 5.1 定義済みの値

CSS の `mix-blend-mode` と同一の集合とします。

```
normal, multiply, screen, overlay, darken, lighten,
color-dodge, color-burn, hard-light, soft-light,
difference, exclusion, hue, saturation, color, luminosity
```

この集合は、CSS、OS コンポジタ、GPU、CPU 合成のいずれの展開経路でも実現できることを基準に選んでいます。

### 5.2 未知の値の扱い

**定義済みでない値に遭遇した実装は、`"normal"` として扱わなければなりません。** エラーとしてはなりません（F1 の考え方と一貫させます）。

### 5.3 実装の対応範囲

`"normal"` 以外の合成モードの実装は**任意**です。対応しない実装は `"normal"` として描画してよいものとします。

現行の実配布ファイルに `"normal"` 以外の使用例はありません。

### 5.4 書き出し側の要件

PSD の合成モード名は CSS の名称と一致しません。書き出し側は 5.1 の名称へ変換しなければならず、**対応するものが無い場合は `"normal"` を用いなければなりません**。定義済みでない名称をそのまま書き出してはなりません。

---

## 6. テクスチャの追加規定（規範的・確定）

v0.3.0 が未規定としていた項目（v0.3.0 仕様 10.1）を確定させます。

| 事項 | v0.4.0 の規定 |
|---|---|
| ビット深度 | **チャンネルあたり 8bit**。16bit の PNG を書き出してはならない |
| 色空間 | **sRGB** |
| ICC プロファイル | 埋め込まれていてもよいが、**実装は解釈しなくてよい**。sRGB として扱ってよい |

カラーマネジメントを要求すると、すべての展開経路に実装項目が増える一方、現時点で必要とする用途がないためです。

---

## 7. v0.3.0 からの変更一覧

| 種別 | 対象 | 内容 |
|---|---|---|
| 新規 | 1 章 | 互換性の規則（B1〜B5、F1〜F5、不在時の意味の表） |
| 新規 | `requiredExtensions[]` | ルートの任意フィールド。v0.4.0 では識別子を定義しない |
| 新規 | `layers[].anchor_x` / `anchor_y` | 任意。v0.4.0 では描画に影響しない |
| 変更 | `sprites[].fps` | 必須 → 任意。不在時は `12` |
| 確定 | `blendMode` | 許容値を CSS `mix-blend-mode` 相当に確定。未知の値は `normal` |
| 確定 | テクスチャ | 8bit / sRGB / ICC は解釈不要 |

**スキーマの破壊的変更はありません。** v0.3.0 のファイルはすべて有効です。

---

## 8. 未決事項

以下は v0.4.0 の範囲では確定させません。

| # | 事項 | 備考 |
|---|---|---|
| U1 | 1.2 の規則が既存 6 実装の実挙動とどの程度一致するか | **実測が必要**（9.2）。実測結果によっては F2〜F4 を見直す |
| U2 | 移行期間の扱い | 全実装の v0.4.0 対応を待って v0.5.0 ファイルを配布するか、`requiredExtensions` による拒否に任せるか |
| U3 | v0.5.0 の機能追加項目 | `frameName` / `control` / `defaultVisible` / `presets` / `tracks` / `sequence.keys` |

---

## 9. 適合性

### 9.1 実装のチェックリスト

適合実装は次をすべて満たさなければなりません。

- [ ] `version` を解釈の分岐に使っていない（B1）
- [ ] 未知のルートキー・未知のフィールドで失敗しない（F1）
- [ ] `parts[].type` が未知の値のとき、`default` の有無で `switch` / `static` を選ぶ（F2）
- [ ] `sequence.type` が未知の値のとき `ordered` として扱う（F3）
- [ ] `trigger.type` が未知の値のとき自律発火しない（F4）
- [ ] `requiredExtensions[]` に未知の識別子があれば拒否し、理由を提示する（F5）
- [ ] `anchor_x` / `anchor_y` を読み取り、内部モデルに保持する（3.2）
- [ ] `fps` の不在を許容し、`12` として扱う（4.2）
- [ ] 未知の `blendMode` を `"normal"` として扱う（5.2）
- [ ] v0.3.0 仕様の適合要件（コンテナ・複数アトラス・座標系・描画順序・識別子の一意性）を満たす

### 9.2 検証

**U1 の実測を、1.2 の規定文を確定させる前に行わなければなりません。**

#### 実測の範囲

実行環境の制約により、全 6 実装を実測することはできません。次の方針を採ります。

| 実装 | 方法 |
|---|---|
| `emg-cdn/emg-player.0.3.0.js` | 実測（ブラウザ） |
| `emg-web-runtime` | 実測（ブラウザ） |
| `emg-ymm4`（`Emg.Core`） | 実測（.NET コンソール） |
| `emg-renpy` | 実測（`init python` ブロックを抽出して実行）。Ren'Py エンジン固有の挙動は対象外 |
| `emg-unity-importer` | **コード読解による推定**（Unity エディタなし） |
| `emg-godot` | **コード読解による推定**（Godot なし） |

推定に留まる 2 実装については、**推定であることを記録に残し**、当該環境が利用可能になった時点で実測へ置き換えなければなりません。

#### 検証パターン

`samples/senti.emg` を基に次の異常系ファイルを生成し、挙動を記録します。

| # | 入力 | 期待（v0.4.0 適合時） |
|---|---|---|
| A | 未知のルートキーを追加 | 無視して読み込む |
| B | `parts[].type` を未知の値に変更（`default` あり） | `switch` として扱う |
| C | `parts[].type` を未知の値に変更（`default` なし） | `static` として扱う |
| D | `sequence.type` を未知の値に変更 | `ordered` として扱う |
| E | `trigger.type` を未知の値に変更 | 自律発火しない |
| F | `sprites[].fps` を削除 | `12` として継続 |
| G | `layers[]` に未知フィールドを追加 | 無視して読み込む |
| H | `requiredExtensions: ["EMG_unknown"]` を追加 | 読み込みを拒否する |
| I | `blendMode` を未知の値に変更 | `"normal"` として描画 |

規定を先に確定させると、実測で「既存実装の多数派と食い違う規定」になった場合に手戻りが生じます。

### 9.3 リグレッション

`samples/senti.emg`（v0.3.0）の描画結果が v0.4.0 実装で変化しないことを確認しなければなりません。ZIP を展開して任意の 2D ライブラリで合成した結果のハッシュを固定する方法で足り、実行環境に依存しません。

---

## 10. バージョン履歴

| バージョン | 変更内容 |
|---|---|
| 0.3.0 | `mapping.json` の追加。2026-08-23 改訂で記載漏れを明文化（スキーマ変更なし） |
| 0.4.0 (Draft) | 互換性の規則を新設。`requiredExtensions[]` と `layers[].anchor_*` を追加。`sprites[].fps` を任意化。`blendMode` の許容値とテクスチャのビット深度・色空間を確定。**表現力の追加および破壊的変更はなし** |
