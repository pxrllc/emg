# EMG 機能識別子 登録簿

**更新日:** 2026-08-24
**関連:** `emg-json-spec-0.4.0.md` §2（`requiredExtensions[]`）

---

## 1. この文書の役割

`data.json` のルートフィールド `requiredExtensions[]` に書ける**機能識別子**の一覧です。

識別子は EMG のバージョンをまたいで追加されるため、各バージョンの仕様書ではなく本書で一元管理します。実装は本書に載っている識別子のうち、自身が理解できるものの集合を持ちます。

---

## 2. 規則（規範的）

### 2.1 命名

接頭辞 `EMG_` に、小文字のスネークケースを続けます。

### 2.2 宣言してよい条件

**識別子は「その機能を理解しない実装が、誤った絵を描いてしまう」場合にのみ宣言します。**

無視しても絵として成立する劣化（動かない、常に表示される、初期状態のままになる）に対して宣言してはなりません。不必要に古い実装を締め出すことになります。

| 劣化の種類 | 宣言 |
|---|---|
| 何も描画されない / 別のものが描画される | **必要** |
| アニメーションしない（静止する） | 不要 |
| 切り替えられない（初期状態のまま） | 不要 |
| 追加情報が無視される（UI に出ないだけ） | 不要 |

### 2.3 読み込み実装の義務

`requiredExtensions[]` に自身が理解しない識別子が 1 つでも含まれる場合、**読み込みを拒否し、理由として当該識別子を提示しなければなりません**（v0.4.0 §2.2）。

本書に載っていない識別子も、同様に「理解しない識別子」として扱います。

### 2.4 追加の手順

新しい識別子を定義するときは、本書に 1 行を追加し、次を明記します。

- 識別子
- 導入バージョン
- 対象となる機能
- 宣言が必要になる**具体的な条件**
- 理解しない実装で何が起きるか

---

## 3. 登録簿

| 識別子 | 導入 | 対象機能 | 宣言が必要な条件 |
|---|---|---|---|
| `EMG_frame_name` | v0.5.0 | `layers[].frameName`（`emg-json-spec-0.5.0.md` §2） | **1 つのフレーム識別子に 2 枚以上のレイヤーが属する場合** |
| `EMG_switch_none` | v0.5.0 | `parts[].defaultVisible`（`emg-json-spec-0.5.0.md` §4.3） | **`type: "switch"` のパーツに `defaultVisible: false` を指定した場合** |

### 3.1 `EMG_frame_name`

**理解しない実装で起きること:** `parts[].default` および `sequence.frames[]` の値を `textureID` として解決しようとし、一致するレイヤーが存在しないため**何も描画されません**。

**宣言が不要な場合:** すべてのフレーム識別子がレイヤー 1 枚のみを持つとき（`frameName` が単なる別名として使われているとき）。この場合、未対応の実装も `textureID` で正しく解決できます。

### 3.2 `EMG_switch_none`

`switch` パーツを初期状態で「どのフレームも表示しない」状態にする機能です。頬の赤み（チーク・青ざめ）や汗・涙のように、**排他バリエーションでありながら「どれも出ていない」のが常態**である対象に使います。

**理解しない実装で起きること:** `defaultVisible` を未知のフィールドとして無視し（v0.4.0 F1）、`emg-json-spec.md` §4.2 の規則どおり `default` のフレームを表示します。結果として**出ないはずのものが常に出ます**。チークを消せないのではなく、消しているつもりのチークが出続けるため、登録簿 §2.2 の「別のものが描画される」に該当します。

**宣言が不要な場合:** `defaultVisible: false` を `type: "static"` のパーツにのみ使っているとき。この場合、未対応の実装はそのパーツを常に表示するだけで、絵としては成立します（帽子を外せないだけ）。

**宣言してはならない場合:** 上記のとおり `static` のみでの使用。不必要に既存の実装を締め出すことになります。

---

## 4. 予約済み・未定義

現時点で本書が定義する識別子は 3 章の 2 件です。

v0.4.0 が追加した機能（`requiredExtensions` 自体、`anchor_*`、`fps` の任意化、`blendMode` の値の確定）には識別子がありません。いずれも理解しない実装が無視しても表示が成立するためです。

v0.5.0 が追加した機能のうち `control` / `presets[]` / `sequence.keys[]` / `tracks[]` にも識別子はありません。理由は同じです（`emg-json-spec-0.5.0.md` §8.3）。

`defaultVisible` は**使い方によって分かれる**唯一の機能です。`static` では宣言不要、`switch` では必須（§3.2）。

---

## 5. 変更履歴

| 日付 | 内容 |
|---|---|
| 2026-08-24 | 新規作成。`EMG_frame_name` を登録 |
| 2026-08-25 | `EMG_switch_none` を登録（`emg-json-spec-0.5.0.md` §4.3 の改訂に伴う） |
