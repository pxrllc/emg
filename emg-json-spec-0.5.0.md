# EMG フォーマット 仕様書 0.5 系列（Draft）

**現行版:** 0.5.3（Draft）
**更新日:** 2026-08-27
**基準:** `emg-json-spec.md`（v0.3.0）、`emg-json-spec-0.4.0.md`（v0.4.0）

---

## 0. この文書について

> **現行版: 0.5.3**（2026-08-27）
>
> 本書は **0.5 系列**を定義します。ファイル名は系列が始まった版のままですが、
> 内容は常に最新版を表します。版ごとの差は 12 章の履歴を参照してください。

### 0.1 差分仕様である

本書は **v0.4.0 からの差分のみ**を定義します。記述のない事項は v0.4.0 および v0.3.0 がそのまま適用されます。矛盾する場合は本書が優先します。

**v0.4.0 の互換性規則（1 章）を前提としています。** v0.4.0 に対応していない実装に対して 0.5 系列のファイルを配布した場合の挙動は保証されません。

### 0.1.1 版の上げ方（アルファ公開まで）

**アルファ公開までは、機能の追加をまとめるたびに 0.5.x の x を 1 つ上げます。**
追加の大小で区別はしません。`0.6.0` は開きません。

公開前は、前方互換の規則・列挙値の意味・規範的な「無視せよ」をまだ撤回できる唯一の期間です。0.5.1 はその実例で、初版の「`defaultVisible` は `switch` では無視しなければならない」を撤回しています。公開後は、既に配布されたファイルを取り残すことになるため同じ変更はできません。

版を上げるときは、本書の履歴（12 章）、`emg-extensions-registry.md`、生成側が書き出す `version`、`tools/emg-validate.js` を同じ回で更新します。

### 0.2 v0.5.0 が追加するもの

v0.4.0 が土台の整備だったのに対し、v0.5.0 は**表現力を追加します**。

| 節 | 追加 | 解決する制約 |
|---|---|---|
| 2 | `layers[].frameName` | 複数レイヤーが同時に切り替わる組を表現できない |
| 3 | `parts[].control` | 利用者が選ぶ切り替えと時間軸が切り替えるものを区別できない |
| 4 | `parts[].defaultVisible` | 独立した on/off（眼鏡・帽子）を表現できない |
| 5 | `presets[]` | 複数パーツの組み合わせをまとめて指定できない |
| 6 | `sequence.keys[]` | フレームの表示時間を個別に指定できない |
| 7 | `sprites[].tracks[]` | 座標変換のアニメーションが存在しない |

### 0.3 段階的に導入してよい

2〜6 節（状態表現）と 7 節（トランスフォーム）は独立しています。**7 節は他の全項目を合わせたより実装コストが大きい**ため（7.9）、2〜6 節のみを先に実装しても v0.5.0 として整合します。

その場合、`tracks` を持つファイルを読んだ実装は当該 sprite を無視し、静止した絵を描きます。これは許容される劣化です（8.2）。

---

## 1. フレーム識別子（規範的・新規概念）

v0.5.0 は、レイヤーを指す参照の意味を 1 段抽象化します。**これが本バージョンで最も広範囲に影響する変更です。**

### 1.1 定義

**フレーム識別子**とは、`switch` パーツ内で「同時に表示される 1 組のレイヤー」を指す文字列です。

レイヤーのフレーム識別子は次で決まります。

1. `layers[].frameName` があればその値
2. なければ `layers[].textureID`

### 1.2 参照箇所

**フレーム識別子で解決しなければならない参照**は次のとおりです。

| 参照元 | v0.4.0 までの意味 | v0.5.0 の意味 |
|---|---|---|
| `parts[].default` | `textureID` | **フレーム識別子** |
| `sequence.frames[]` | `textureID` | **フレーム識別子** |
| `sequence.keys[].frame` | （新規） | フレーム識別子 |
| `presets[].parts` の値 | （新規） | フレーム識別子 |
| `mapping.json` の `blink.{open,half,closed}` | `textureID` | **フレーム識別子** |
| `mapping.json` の `lipSync.{a,i,u,e,o,n,open}` | `textureID` | **フレーム識別子** |
| `mapping.json` の `expressions[].parts` の値 | `textureID` | **フレーム識別子** |
| `mapping.json` の `expressions[].other[]` | `textureID` | **フレーム識別子** |

### 1.3 後方互換

**`frameName` を持たないファイルでは、フレーム識別子は `textureID` と同一です。** したがって v0.4.0 以前のファイルの解決結果は一切変わりません。

この定義により、`frameName` の導入が `mapping.json` の意味を壊さずに済みます。`frameName` を使うパーツでは `mapping.json` 側も自動的にフレーム識別子を指すことになり、両者の間に変換規則を設ける必要がありません。

> **`emg-mapping-spec.md` との関係:** 同仕様書は改訂済みで、レイヤーを指す値をフレーム識別子として解決すると定めています（同書「レイヤーの参照方法」）。値の書き方は変わりません。

---

## 2. `layers[].frameName`（規範的・新規）

### 2.1 定義

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `frameName` | string | ❌ | このレイヤーが属するフレームの名前。不在時は `textureID` と同値 |

### 2.2 意味論

`switch` パーツの表示単位は、**レイヤー 1 枚ではなくフレーム識別子 1 つ**になります。

適合実装は、`switch` パーツについて次のように解決しなければなりません。

1. 表示するフレーム識別子を 1 つ決める（v0.3.0 仕様 4.2 の優先順位に従う）
2. **そのフレーム識別子を持つレイヤーを、パーツ内から*すべて*表示する**
3. 他のレイヤーは表示しない

各レイヤーは自身の `textureZIndex` に従って描画されます。同一フレームのレイヤーが連続した z を持つ必要はありません。

### 2.3 例

上着とスカートが同時に切り替わる衣装。両者の間に体（z=10）が挟まります。

```json
{
  "partID": "costume",
  "type": "switch",
  "control": "user",
  "default": "uniform",
  "layers": [
    { "textureID": "jacket_uni", "frameName": "uniform", "textureZIndex": 20, "...": "..." },
    { "textureID": "skirt_uni",  "frameName": "uniform", "textureZIndex": 5,  "...": "..." },
    { "textureID": "jacket_cas", "frameName": "casual",  "textureZIndex": 20, "...": "..." },
    { "textureID": "pants_cas",  "frameName": "casual",  "textureZIndex": 5,  "...": "..." }
  ]
}
```

### 2.4 この機構が必要な理由

代替手段である「複数レイヤーを 1 枚の画像に焼き込む」は、**対象レイヤーの `textureZIndex` が連続している場合にのみ成立します**。

| 例 | z の連続性 | 焼き込み |
|---|---|---|
| 両目（`eye_L` / `eye_R`） | 隣接 | 成立する |
| 上記の衣装（上着 z=20 / 体 z=10 / スカート z=5） | **非連続** | **原理的に成立しない** |

したがって、間に他のパーツを挟む組み合わせは `frameName` なしには表現できません。

### 2.5 `textureID` の一意性

`frameName` の導入後も、`textureID` は**パーツ内で一意でなければなりません**（v0.3.0 仕様 6 章）。同一フレームに属するレイヤーどうしも、異なる `textureID` を持ちます。

### 2.5.1 PSD からの書き出し（参考）

`emg-packer` / `emg-web-packer` は、**グループ名の先頭が `@` のグループを「フレームグループ」**として扱い、その配下のレイヤーに `frameName` を付けます。`@` の無いグループは従来どおりパーツです。

```
衣装          （グループ）    → partID = 衣装
  @制服       （グループ）    → frameName = 制服
    上着
    スカート
  @私服       （グループ）    → frameName = 私服
    シャツ
    ズボン
```

接頭辞という目印を要するのは、パッカーの走査規則が「どのグループ階層も `partID` を上書きする」形であり、**階層の深さだけでは `frameName` と区別できない**ためです。opt-in にすることで、既存の PSD の解釈は一切変わりません。

`requiredExtensions` は 2.6 の条件に従って自動で付与されます（1 フレームに 2 枚以上属する場合のみ）。

**PSD で非表示だった `static` パーツの扱い（§4）:**

| PSD の状態 | 書き出し |
|---|---|
| パーツ内の**全レイヤーが非表示** | パーツごと書き出し、`defaultVisible: false` を付ける（初期非表示のトグルになる） |
| 一部だけ非表示 | その隠れレイヤーを書き出さない（常時表示の `static` に混ぜると意図しない重なりになるため） |

v0.4.0 以前は前者も丸ごと捨てられていたため、帽子や眼鏡のような「PSD では隠してあるが後から出したいパーツ」が失われていました。

### 2.6 `requiredExtensions` の宣言（規範的）

**あるフレーム識別子に 2 枚以上のレイヤーが属する場合、ファイルは `requiredExtensions` に `"EMG_frame_name"` を含めなければなりません。**

理由: 未対応の実装は `parts[].default` を `textureID` として解決しようとし、一致するレイヤーが存在しないため**何も描画しません**。衣装の例では上着だけが表示される、といった中途半端な絵になるより、明示的に失敗するほうが安全です。

すべてのフレーム識別子がレイヤー 1 枚のみを持つ場合（`frameName` が単なる別名として使われている場合）、宣言は不要です。この場合、未対応の実装も `textureID` で正しく解決できます。

---

## 3. `parts[].control`（規範的・新規）

### 3.1 定義

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `control` | `"animated"` \| `"user"` | ❌ | このパーツを切り替える主体。不在時は `"animated"` |

| 値 | 意味 |
|---|---|
| `animated` | 主に `sprites[]` や `mapping.json` が時間軸に沿って切り替える。目・口など |
| `user` | 主に利用者が選択する。衣装・髪型など |

### 3.2 制約ではなくヒントである（規範的）

**`control` は制約ではありません。** `control: "user"` のパーツを `sprites[]` が切り替えることも、`control: "animated"` のパーツを利用者が固定することも許容されます。

制約にすると、演出目的で衣装を切り替えるといった正当な用途を禁じることになるためです。

実装は `control` を無視してよいものとします。主な用途は UI であり、「衣装」を表情の切り替え欄ではなく設定欄に出す、といった提示の分岐に用います。

---

## 4. `parts[].defaultVisible`（規範的・新規）

### 4.1 定義

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `defaultVisible` | boolean | ❌ | 初期状態で表示するか。不在時は `true` |

`static` / `switch` の**両方**に意味を持ちます。意味論は型ごとに異なります（§4.2 / §4.3）。

> **0.5.1 での改訂（2026-08-25）**
> 0.5.0 では「`switch` パーツに指定された場合は無視しなければならない」と規定していました。
> これを撤回します。理由は §4.3.1 に記します。

### 4.2 `static` パーツでの意味論

`defaultVisible` を持つ `static` パーツは、**独立して on/off を切り替えられる対象**（眼鏡・帽子・小物）を表します。

パーツ内の全レイヤーが一括して表示・非表示になります。帽子と帽子の影のように複数レイヤーからなる組も、1 つのパーツにまとめることで同時に切り替わります。

```json
{ "partID": "hat", "type": "static", "defaultVisible": false,
  "layers": [
    { "textureID": "hat",        "textureZIndex": 40, "...": "..." },
    { "textureID": "hat_shadow", "textureZIndex": 39, "...": "..." }
  ] }
```

### 4.3 `switch` パーツでの意味論（規範的・改訂）

`defaultVisible: false` を持つ `switch` パーツは、**初期状態でどのフレームも表示しません**。

`emg-json-spec.md` §4.2 は「`switch` パーツは `layers` のうちちょうど 1 枚を排他表示する」と定めていますが、`defaultVisible: false` のパーツはその適用外です。表示中のフレーム数は **0 または 1** となります。

```json
{ "partID": "Blushs", "type": "switch", "defaultVisible": false,
  "default": "cheek",
  "layers": [
    { "textureID": "cheek", "...": "..." },
    { "textureID": "pale",  "...": "..." },
    { "textureID": "01",    "...": "..." }
  ] }
```

これは**排他バリエーションと、出す／出さないの切り替えが同時に必要な対象**を表します。頬の赤み（チーク・青ざめ）、汗、涙、怒りマークのように、「複数の表現のうち 1 つ」でありながら「どれも出ていない」状態が常態であるものが該当します。

#### 4.3.1 `switch` に適用範囲を広げる理由

この状態は、改訂前の v0.5.0 では表現できませんでした。

- `default` を省略・不一致にする → `emg-json-spec.md` §4.2 の規則 3 により**先頭レイヤーへフォールバック**する。狙いと正反対に、常にチークが出る
- `static` + `defaultVisible: false` にする → `static` は全レイヤーを同時描画するため、チークと青ざめが**重なって**出る
- 透明レイヤーを 1 枚足して `default` にする → 表示上は正しく動くが、パーツの意味論が「透明な絵を描いている」になり、`presets[]` や `mapping.json` から「無し」を指すときの参照先が実データに依存する

排他バリエーションと on/off は直交する軸であり、`type` の値ではなく付加属性で表すという §4.3（旧 §4.3）の判断は `switch` にもそのまま当てはまります。

#### 4.3.2 `default` の意味（規範的）

`defaultVisible: false` の `switch` パーツでも、`default` は**引き続き必須**です。

`default` は「このパーツが表示状態にされたとき、フレームの指定が無ければ選ばれるフレーム」を意味します。初期状態では表示されないため、`default` は初期表示フレームではなくなります。

#### 4.3.3 表示状態の遷移（規範的）

実装は、`switch` パーツについて**「フレーム未選択」状態を表現できなければなりません**。

| 遷移 | 結果 |
|---|---|
| 外部制御がフレームを指定 | そのフレームを表示する（未選択状態を抜ける） |
| 外部制御が「未選択」を指定 | どのフレームも表示しない |
| `presets[]` が当該パーツを含まない | 状態を変えない |

`defaultVisible: true`（既定）の `switch` パーツを「未選択」にできるかは実装依存とし、本書では要求しません。

### 4.4 アニメーションとの関係（規範的）

`defaultVisible: false` により非表示となっているパーツを `targetPartID` に持つ `sprites[]` は、**発火してはなりません**。パーツが表示状態に戻った時点で通常どおり動作します。

これは `static` / `switch` の両方に適用されます。`mapping.json` の blink / lipSync が非表示の `switch` パーツを対象にしている場合も同様に、そのパーツを表示状態にするまで解決結果を適用してはなりません。

### 4.5 新しい `type` を導入しない理由

`type: "toggle"` を追加する案もありましたが採りません。

`type` の値は 6 つの消費側実装すべてで分岐に使われており、新しい値を追加すると未対応の実装で挙動が定まりません。付加属性とすることで、`static` については未対応の実装が `defaultVisible` を無視して**常に表示**します。帽子を外せないだけで絵は成立するため、劣化として許容できます。

代償として、3 種の切り替え（アニメーション差し替え / 排他バリエーション / 独立トグル）のうち独立トグルだけが `type` ではなく付加属性で表現されることになり、構造の対称性を欠きます。互換性を優先した判断です。

なお `switch` については同じ許容が成り立ちません（未対応の実装は `default` のフレームを描いてしまい、**出ないはずのものが出る**）。このため `switch` での使用に限り機能識別子の宣言を要求します（§4.7）。

### 4.6 依存関係

眼鏡と髪型の干渉のような**組み合わせの制約は v0.5.0 では扱いません**。競合しない素材を用意することを前提とします。

`conflicts` / `requires` による依存グラフは、組み合わせ検証の実装を全消費側に要求するため、必要性が実データで確認されてから導入します。

### 4.7 `requiredExtensions` の宣言（規範的）

`type: "switch"` かつ `defaultVisible: false` のパーツが 1 つでも存在するファイルは、`requiredExtensions[]` に **`EMG_switch_none`** を含めなければなりません（`emg-extensions-registry.md` §3.2）。

`static` のみで使用している場合は宣言してはなりません。未対応の実装が常に表示するだけで、絵としては成立するためです（登録簿 §2.2）。

---

## 5. `presets[]`（規範的・新規）

### 5.1 定義

ルートオブジェクトに任意フィールドとして追加します。

```json
"presets": [
  {
    "presetID": "school",
    "label": "制服",
    "parts":   { "costume": "uniform", "hairstyle": "short" },
    "toggles": { "glasses": true, "hat": false }
  }
]
```

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `presetID` | string | ✅ | プリセットの識別 ID。ファイル内で一意 |
| `label` | string | ❌ | UI に表示する名称。不在時は `presetID` を用いてよい |
| `parts` | object | ❌ | `partID` → **フレーム識別子** のマップ。対象は `switch` パーツ |
| `toggles` | object | ❌ | `partID` → boolean のマップ。対象は `defaultVisible` を持つ `static` パーツ |

### 5.2 適用の意味論（規範的）

プリセットの適用は**指定された項目のみを変更します**。`parts` / `toggles` に現れない `partID` の状態は変更してはなりません。

プリセットが全項目を確定させる方式は採りません。パーツを 1 つ追加するたびに既存のすべてのプリセットを更新する必要が生じるためです。

### 5.3 `mapping.json` の `expressions` との関係

両者はいずれも「複数パーツの状態をまとめて指定する」ものです。役割を次のように分けます。

| | 担当 | 置き場所 |
|---|---|---|
| `presets[]` | **パーツ状態の組み合わせ**（衣装・髪型・小物） | `data.json`（構造） |
| `expressions` | **意味づけ**（joy / angry と、それが指す状態） | `mapping.json`（意味） |

`mapping.json` の表情は、パーツ状態を直接書く代わりに `presetID` を参照できます。

```json
"expressions": {
  "formal": { "presetID": "school" }
}
```

これにより、パーツ状態の記述が `presets[]` に集約されます。`expressions[].parts` を直接書く方式も引き続き有効であり、両方が指定された場合は `expressions[].parts` が優先します。

> **`emg-mapping-spec.md` との関係:** 同仕様書は改訂済みで、`expressions` の要素に任意フィールド `presetID` が定義されています。`presetID` と `parts` の双方がある場合は `parts` が優先します。

---

## 6. `sequence.keys[]`（規範的・新規）

### 6.1 定義

`sequence` は `frames`（等間隔）または `keys`（不等間隔）のいずれかを持ちます。

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `keys` | Key[] | ❌ | 不等間隔のフレーム列。`frames` と排他 |

Key オブジェクト:

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `t` | number | ✅ | 再生開始からの秒数 |
| `frame` | string | ✅ | フレーム識別子 |

```json
{
  "spriteID": "blink",
  "targetPartID": "Eyes",
  "sequence": {
    "type": "ordered",
    "keys": [
      { "t": 0.00, "frame": "01" },
      { "t": 0.06, "frame": "03" },
      { "t": 0.10, "frame": "04" },
      { "t": 0.18, "frame": "03" },
      { "t": 0.24, "frame": "01" }
    ]
  }
}
```

`keys` を用いる場合、`fps` は不要です。

### 6.2 解決規則（規範的）

時刻 `t` における表示フレームは、**`key.t <= t` を満たす最後のキー**とします。

step 補間として一意に定まります。キーは `t` の昇順に並んでいなければなりません。

### 6.3 出力フレームレートとの関係

上記の規則により、**出力 fps の 1 フレーム間隔より短いキーは表示されません**。

例: 80ms 間隔のキーは 30fps（33.3ms）では表示されますが、12fps（83.3ms）では中割りが消えます。

書き出し側は、想定する最低の出力 fps の 1 フレーム以上の間隔をキーに与えるべきです。

### 6.4 尺

`keys` を用いる sprite の尺は、**最後のキーの `t`** とします。

---

## 7. `sprites[].tracks[]` — トランスフォーム（規範的・新規）

### 7.1 `targetPartID` の制約の変更（規範的）

v0.3.0 は `sprites[].targetPartID` に対し「対象パーツの `type` は `switch` であること」を要求していました。v0.5.0 では制約の対象を変更します。

> **`sequence` を持つ sprite の `targetPartID` は、`type` が `switch` のパーツを指さなければならない。**
> `tracks` のみを持つ sprite は、`static` / `switch` のいずれのパーツも対象にできる。

トランスフォームの対象は体・髪・アクセサリといった `static` パーツであるためです。

この付け替えにより、`mapping.json` との共存規則（v0.3.0 仕様 7.3）の判定条件は変わりません。同規則は `sequence` による自律発火を対象とします。

### 7.2 定義

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `tracks` | Track[] | ❌ | 座標変換のキーフレーム列 |
| `duration` | number | `tracks` を持つ場合は必須 | 尺（秒） |
| `loop` | `"once"` \| `"loop"` \| `"pingpong"` | ❌ | 不在時は `"loop"` |
| `phaseOffset` | number | ❌ | 再生開始位置のずれ（秒）。不在時は `0` |

Track オブジェクト:

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `path` | string | ✅ | 対象プロパティ（7.3） |
| `keys` | TrackKey[] | ✅ | `{ "t": 秒, "v": 値 }` の配列。`t` の昇順 |
| `interpolation` | `"step"` \| `"linear"` \| `"cubic"` | ❌ | 不在時は `"linear"` |

```json
{
  "spriteID": "hair_sway",
  "targetPartID": "hair_back",
  "duration": 3.0,
  "loop": "pingpong",
  "tracks": [
    { "path": "rotation",
      "keys": [ { "t": 0.0, "v": 0 }, { "t": 1.5, "v": 2.5 }, { "t": 3.0, "v": 0 } ],
      "interpolation": "linear" }
  ]
}
```

### 7.3 対象プロパティ（規範的）

| `path` | 単位 | 既定値 |
|---|---|---|
| `translate_x` | px（キャンバス座標） | `0` |
| `translate_y` | px（キャンバス座標） | `0` |
| `rotation` | 度。**時計回りが正** | `0` |
| `scale_x` | 倍率 | `1.0` |
| `scale_y` | 倍率 | `1.0` |
| `opacity` | `0.0`〜`1.0` | `1.0` |

**この 6 種以外を定義しません。** メッシュ変形・色調補正・`textureZIndex` の変更は v0.5.0 の範囲外です。

この集合に限定することで、CSS transform、OS コンポジタ（DirectComposition / Core Animation）、GPU、CPU 合成のいずれの経路でも実現できます。これを超える表現を含めると、いずれの経路でも自前描画が必要になります。

### 7.4 適用順序（規範的）

対象となる各レイヤーへ、次の順で適用しなければなりません。

```
1. アンカー点（anchor_x, anchor_y）を原点へ移動
2. scale_x / scale_y を適用
3. rotation を適用
4. アンカー点を元の位置へ戻す
5. translate_x / translate_y を適用
6. opacity を layer.opacity に乗じる
```

アンカー点は v0.4.0 で定義済みです（不在時は `basePosition` と同値）。

**アンカーはレイヤーごとに独立しています。** 1 番と 4 番で使うのは、いま処理しているレイヤー自身の `anchor_x` / `anchor_y` であり、パーツ共通の 1 点ではありません。したがって 1 本の `rotation` トラックで、髪の房のそれぞれを自分の根元を軸に回す、といった表現ができます。パーツ全体を 1 点を軸に回したい場合は、所属レイヤー全部に同じアンカーを書きます。

同一レイヤーを対象とする sprite が複数存在する場合の合成順序は**未定義**です。書き出し側は 1 つのレイヤーに対して 1 つのトランスフォーム sprite のみを定義すべきです。異なるレイヤーを対象とする sprite が併存することは差し支えありません（7.4.1）。

### 7.4.1 `sprites[].targetLayer`（規範的・0.5.3 で追加）

| フィールド | 型 | 必須 | 説明 |
|---|---|---|---|
| `targetLayer` | string | ❌ | トランスフォームの対象を、`targetPartID` のパーツ内の特定のレイヤーに絞る。値は**フレーム識別子**（1.1） |

不在時は、これまでどおりパーツの全レイヤーが対象です。

```json
{
  "spriteID": "hair_sway",
  "targetPartID": "Body",
  "targetLayer": "Back_hair",
  "duration": 3.0,
  "loop": "pingpong",
  "tracks": [
    { "path": "rotation", "keys": [ { "t": 0, "v": 0 }, { "t": 1.5, "v": 2.5 }, { "t": 3.0, "v": 0 } ] }
  ]
}
```

**規則**

1. `targetLayer` を持つ sprite は `sequence` を持ってはなりません。`sequence` はパーツ内のフレームを切り替えるものなので、対象を 1 つに絞ることと両立しません。
2. 値は `targetPartID` のパーツに実在するフレーム識別子でなければなりません。
3. 同じフレーム識別子を持つレイヤーが複数ある場合（`frameName` でまとめた組）、そのすべてが対象になります。**「まとめて動く一塊」を指す単位はフレーム識別子である**という 1.1 の扱いを、ここでも変えません。
4. パーツのレイヤーが 1 枚しかない場合、`targetLayer` は書くべきではありません。効果が変わらないのに、次項の宣言義務だけが増えます。

**宣言義務。** `targetLayer` を使うファイルは `requiredExtensions` に `EMG_layer_transform` を含めなければなりません（v0.4.0 2 章）。

この機能を理解しない実装は `targetLayer` を無視し、**パーツ全体に**トランスフォームを適用します。髪だけが揺れるはずの絵で体ごと揺れるので、これは「動かない」ではなく「違う絵」です。`emg-extensions-registry.md` の基準（無視すると誤った絵になるものだけを宣言する）に該当します。

### 7.4.2 なぜパーツを分割せずに済ませるのか（非規範的）

同じことは、動かしたいレイヤーを独立した `parts[]` に切り出しても表現できます。実際 0.5.2 まではそれが唯一の方法でした。

それでもこの項を足したのは、**パーツが描画の単位であると同時に、状態と意味づけの単位でもある**ためです。`parts[].type` / `default` / `defaultVisible`、`presets[].parts` と `toggles`、`mapping.json` の `blinkPartKey` / `lipSyncPartKey` はいずれも partID を単位に働きます。揺らしたい房を独立させると、その房は「体」というまとまりから外れ、衣装の着せ替えプリセットが体を指しても房には及ばなくなります。動きの都合で、無関係な状態管理の単位まで割れてしまう。

`targetLayer` は動きの粒度だけをレイヤーへ下げ、状態と意味づけの粒度はパーツのまま保ちます。

### 7.5 補間（規範的）

| 値 | 定義 |
|---|---|
| `step` | 次のキーに達するまで値を保持する |
| `linear` | 線形補間 |
| `cubic` | **Catmull-Rom スプライン**。制御点は持たない。端点は最初／最後のキーを複製して計算する |

`cubic` を Catmull-Rom に固定するのは、追加のデータを必要とせず、実装間で結果が一意に定まるためです。ベジェ制御点による自由な緩急は、データ構造とオーサリング側の要件を増やすため導入しません。

### 7.6 ループ（規範的）

| 値 | 定義 |
|---|---|
| `once` | 1 回再生し、最後のキーの値を保持する |
| `loop` | `duration` 経過後、時刻 0 へ戻る |
| `pingpong` | `duration` 経過後、逆再生する。1 周期は `2 × duration` |

`loop` の場合、**終端と始端は補間しません**。時刻 `duration` の値は時刻 0 の値と一致しているべきです。一致しない場合、ループ時に値が不連続に飛びます。

### 7.7 位相（規範的）

`phaseOffset` は再生開始位置を秒単位でずらします。**決定的な値のみを持ちます。**

同一の `.emg` を画面内に複数配置した際のランダム化は**展開側の責務**とし、フォーマットには含めません。乱数を含めると固定のループ点が存在しなくなり、シームレスループ動画の書き出しと両立しないためです。

### 7.8 `sequence` との併用

1 つの sprite が `sequence` と `tracks` の両方を持つことを**許容します**。この場合、`targetPartID` は `switch` パーツでなければなりません（7.1）。

### 7.9 実装コストに関する注記（参考）

**7 節は、2〜6 節を合わせたより実装コストが大きくなります。**

2〜6 節は「どのレイヤーを描くか」の判断が変わるだけで、描画処理そのものは v0.3.0 のまま（等倍・軸並行の矩形転送）です。7 節は行列変換と毎フレーム更新を持ち込みます。

| 実装 | 見込み |
|---|---|
| `emg-unity-importer` | `Transform` がある。容易 |
| `emg-godot` | `Sprite2D` が変換を持つ。容易 |
| `emg-ymm4` | Direct2D の行列変換。既存の `TransformMatrix` の延長 |
| `emg-cdn` プレイヤー | CSS transform へ対応可能。アンカーを `transform-origin` に写す |
| `emg-web-runtime` | Canvas の毎フレーム再描画が新規に必要 |
| `emg-renpy` | **`im.*` は静的画像の合成。ATL / Transform への作り替えという設計変更** |

0.3 のとおり、7 節を後回しにして 2〜6 節のみを実装しても v0.5.0 として整合します。

---

## 8. 互換性

### 8.1 v0.4.0 ファイルとの関係

**v0.4.0 以前のファイルはすべて、変更なしに v0.5.0 のファイルとして有効です。** 本書が追加するフィールドはすべて任意であり、不在時の意味が従来の挙動と一致します。

### 8.2 不在時の意味（v0.4.0 §1.3 への追加）

| フィールド | 不在時の解釈 | 従来の挙動と一致 |
|---|---|---|
| `layers[].frameName` | `textureID` と同値 | ○ |
| `parts[].control` | `"animated"` | ○ |
| `parts[].defaultVisible` | `true` | ○ |
| `presets[]`（ルート） | 空配列 | ○ |
| `sequence.keys[]` | 不在（`fps` + `frames` を用いる） | ○ |
| `sprites[].tracks[]` | 不在（変換なし） | ○ |
| `sprites[].duration` | `tracks` 不在時は `frames.length / fps` または最後の `key.t` | ○ |
| `sprites[].loop` | `"loop"` | ○ |
| `sprites[].phaseOffset` | `0` | ○ |
| `tracks[].interpolation` | `"linear"` | ○ |

### 8.3 `requiredExtensions` の宣言（規範的）

v0.5.0 が定義する機能識別子は **`EMG_frame_name`** と **`EMG_switch_none`** の 2 件です（`emg-extensions-registry.md`）。

| 追加 | 宣言 | 未対応実装での劣化 |
|---|---|---|
| **`frameName`（2 枚以上をまとめる場合）** | **必須** | 何も描画されない。宣言により明示的に失敗させる |
| `frameName`（1 枚のみ） | 不要 | `textureID` として正しく解決される |
| `control` | 不要 | 無視される。表示は変わらない |
| `defaultVisible`（`static`） | 不要 | 常に表示される。絵は成立する |
| **`defaultVisible`（`switch`）** | **必須** | `default` のフレームが出る。出ないはずのものが出る |
| `presets[]` | 不要 | 無視される。既定の状態で表示される |
| `sequence.keys[]` | 不要 | 当該 sprite が動かない。静止した絵になる |
| `tracks[]` | 不要 | 変換が適用されない。静止した絵になる |

**宣言は「無視すると誤った絵になる」場合にのみ行います。** 動かないだけで絵として成立する劣化に対して宣言すると、不必要に古い実装を締め出すことになります。

---

## 9. v0.5.0 で扱わない事項

| 事項 | 理由 |
|---|---|
| 親子関係（ボーン階層） | 実装コストが大きい。アクセサリの追従は同じキーを複製することで代替できる |
| メッシュ変形 / IK | 仕様・実装・制作のすべてに大きな追加を要する |
| `textureZIndex` のアニメーション | 腕を体の前後に動かす等ができないが、描画順序の動的変更は全経路で実装コストが高い |
| トグルの依存関係（`conflicts` / `requires`） | 4.4 |
| クリップの起動条件（状態遷移） | 再生タイミングは各アプリケーションが決める。EMG は「どの sprite が存在するか」までを持つ |
| 複数プロファイルの共存 | `mapping.json` はファイル名が固定の単一ファイル。命名規約の設計が別途必要 |
| 個別画像への移行 | アトラス維持を前提とする（v0.3.0 仕様 1.3） |

---

## 9.1 実装状況（0.5.1 時点・2026-08-25）

| 実装 | 1〜2 frameName | 3〜5 | 4.3 switch none | 6 keys | 7 tracks |
|---|---|---|---|---|---|
| `emg-ymm4`（`Emg.Core`） | ✅ | ✅ | ✅ | ✅ | ✅ 評価器 |
| `emg-cdn` プレイヤー | ✅ | ✅ | ✅ | ✅ | ✅ CSS transform |
| `emg-godot` | ✅ | ✅ | ✅ | ✅ | ✅ `_process()` |
| `emg-unity-importer` | ✅ | 一部 | ✅ | — | ✅ 評価器 |
| `emg-web-runtime` | ✅ | 一部 | ✅ | — | ✅ 評価器 |
| **`emg-renpy`** | ✅ | 一部 | ✅ | — | **非対応（方針）** |

- 4.3 の実装で、`switch` を「未選択」にできることと、外部制御でフレームを
  指定したら未選択を抜けられることの両方が要件です（4.3.3）。
  `emg-godot` はここで 2 か所の作り替えを要しました。非表示パーツへの
  `switch_texture()` が早期 return していたため後からチークを出せず、
  パーツを表示に戻す処理が全レイヤーを表示していたため差分が重なっていました。
- `emg-renpy` は呼び出し側が `show` するまで何も表示されないため、
  初期非表示は追加の実装なしに満たされます。

- **`emg-renpy` の 7 章は対応しません。** `im.*` は静的画像の合成であり、
  トランスフォームには ATL / Transform への作り替えという設計変更を要します。
  0.3 のとおり 2〜6 章のみの実装でも v0.5.0 として整合します。
- `emg-unity-importer` / `emg-web-runtime` / `emg-renpy` の「一部」は、
  `presets` を実装していないことを指します（`sprites[]` 再生を持たないため
  6 章も対象外）。
- 「評価器」は §7 の値解決（`ResolveAt` 相当）まで実装済みで、
  実際の描画への適用は各ホスト側に委ねている状態を指します。

検証は `emg-ymm4` と `emg-cdn` プレイヤーが実測、`emg-renpy` がロジック実行、
`emg-unity-importer` が構文検査、`emg-web-runtime` が型検査、
`emg-godot` は括弧・字下げの整合のみです（GDScript パーサが利用できないため）。

---

## 10. 実装間で結果を一致させるための規定（規範的・0.5.2 で追加）

v0.3.0 から 0.5.1 までは、**複数の実装が同じファイルから同じ絵を出すために必要な前提**のうち、いくつかが未定義のまま残っていました。本章はそれを埋めます。

新しい表現力は追加しません。**これまで「実装依存」だったものを確定させるだけ**です。既存のファイルの意味は変わりません。

### 10.1 座標系（H8）

**キャンバス座標もアトラス座標も、左上を原点とし、X は右、Y は下を正とします。** 単位は px。

| 対象 | 原点 |
|---|---|
| `baseCanvasWidth` / `Height` が張る空間 | 左上 (0, 0) |
| `basePosition_x` / `_y`、`anchor_x` / `_y` | 同上 |
| `layers[].x` / `y`（アトラス内） | アトラス画像の左上 (0, 0) |

Y 軸を上向きに扱うエンジン（Unity 等）へ移植する場合、**読み込み時に一度だけ変換**してください。仕様側は画像フォーマットとしての自然な向きに固定します。

`tracks` の `rotation` は**時計回りが正**です（§7.3）。これは Y 下向きの座標系での右回りに一致します。

### 10.2 色空間とアルファ（H1・H2）

**合成は sRGB のまま行います。リニア空間へ変換してはなりません。**

`blendMode` は CSS の `mix-blend-mode` と同じ値集合であり（v0.4.0 §5）、CSS 自体が sRGB のまま合成するためです。リニア変換を挟むと `multiply` / `screen` などの結果が実装ごとに変わります。

**アルファは straight（非乗算済み）です。** PNG の規定どおりで、`.emg` に premultiplied のテクスチャを入れてはなりません。

実装が内部で premultiply することは許容されます（Direct2D など、そうでないと扱えない経路があるため）。ただし**最終的な結果は straight で合成した場合と一致しなければなりません**。

### 10.3 描画順が同値のとき（H3）

v0.3.0 は `textureZIndex` が同値の場合の順序を未定義としていました。**これを確定させます。**

> **`textureZIndex` が同値のレイヤーは、宣言順に描画しなければなりません。**
> 宣言順とは、`parts[]` の並び順、その中の `layers[]` の並び順を指します。

安定ソートを用いれば自然に満たされます。未定義のままだと、不安定ソートを使った実装だけが違う絵を出します。

### 10.4 `sequence` と `tracks`（H4・H6・H7）

#### 10.4.1 併用できる

1 つの sprite が `sequence` と `tracks` の両方を持つことは**許容されます**。両者は**独立に評価します**。

| | 時間の基準 | 繰り返し |
|---|---|---|
| `sequence` | `fps` または `keys[].t` | `trigger` が決める（10.4.2） |
| `tracks` | `duration` | `loop` が決める |

`phaseOffset` は `tracks` にのみ効きます。

#### 10.4.2 `loop` は `tracks` 専用（H6）

**`loop` は `sequence` に適用されません。** `sequence` の繰り返しは `trigger` が決めます。

| `trigger.type` | `sequence` が末尾に達したとき |
|---|---|
| `auto_loop` | ただちに先頭から再開する |
| `random_interval` | **末尾のフレームを保持**し、次の発火まで待つ |
| `external` / 不在 | **末尾のフレームを保持**する |

#### 10.4.3 `fps` と `keys` の優先順位（H7）

**`sequence.keys` があるとき、`fps` は無視しなければなりません。** 両方を書いてはなりません（生成側の制約）が、遭遇した場合の解釈はこの規則で一意に定まります。

### 10.5 同じパーツを複数の sprite が対象にしたとき（H5）

#### 10.5.1 生成側の制約

**同一の `targetPartID` を対象とし、かつ自律発火する（`trigger` が `external` 以外）`sequence` を持つ sprite を、複数書いてはなりません。** 表示できるフレームは 1 つなので、取り合いになります。

#### 10.5.2 読み込み側の解決

上記に違反したファイルに遭遇した場合、および外部制御で複数が同時に有効になった場合、**`sprites[]` の宣言順で後にあるものを優先しなければなりません**。

`tracks` についても同様です。**同一パーツの同一 `path` に複数のトラックが効く場合、宣言順で後のものが上書きします。加算してはなりません。**

加算にすると、実装ごとの評価順の違いがそのまま値の違いになります。

#### 10.5.3 `external` の再発火（M2）

再生中の sprite に対して同じ `spriteID` の再生要求が来た場合、**先頭から再生し直します**。無視やキューイングを行ってはなりません。

### 10.6 参照の欠落（M3）

`presets[].parts` / `toggles` が、存在しない `partID` を指している場合、**その項目を無視して残りを適用します**。読み込み全体を失敗させてはなりません。

`presets[].parts` が指すフレーム識別子が対象パーツに存在しない場合も同様に、その項目のみ無視します。

生成側はこれらを書いてはなりません（`tools/emg-validate.js` が検査します）。

### 10.7 数値（M5・L3）

- `x` / `y` / `width` / `height` / `basePosition_*` / `anchor_*` / `textureZIndex` は**整数**です。
- `opacity` と `tracks` の値の内部精度は実装に委ねます。倍精度で扱ってかまいません。
- **`opacity` が 0.0〜1.0 の範囲外の場合、その範囲へクランプします**（L3）。
- **ピクセル完全一致は要求しません。** 補間やアンチエイリアスの差は許容されます。要求するのは「同じ絵に見えること」であり、ハッシュ一致ではありません。

### 10.8 異常系（M6・L1）

| 状況 | 要求される挙動 |
|---|---|
| `textures[]` が宣言するファイルが ZIP 内に無い | **読み込みを失敗させる** |
| PNG が復号できない | **読み込みを失敗させる** |
| メイン JSON が見つからない | **読み込みを失敗させる** |
| `requiredExtensions[]` に未知の識別子 | 読み込みを拒否する（v0.4.0 F5） |

**一部のレイヤーを黙って描かずに続行してはなりません。** 沈黙して誤った絵を出すより、検知できる状態を優先します（v0.4.0 §1.2 と同じ判断）。

**ZIP のエントリ名は大文字小文字を区別します**（L1）。生成側と消費側の OS が異なる場合でも一致するよう、比較で正規化してはなりません。

### 10.9 アトラスのサンプリング（参考・非規範的）（M1）

`.emg` のアトラスは矩形を密に詰めており、**パディングを持ちません**。等倍描画では問題になりませんが、`tracks` の `scale_x` / `scale_y` で拡大した場合や、バイリニアでサンプリングした場合に、隣接する領域の色が滲むこと（ブリード）があります。

v0.5 系列ではパディングを要求しません。拡大を前提とする素材では、**生成側が 1〜2px の余白を入れる**ことで回避できます。サンプリング方式（最近傍／バイリニア）は実装に委ねます。

### 10.10 `mapping.json` のバージョンと `avatarId`（M4・L2）

**`mapping.json` は独自のバージョンを持ちません。** 同じ `.emg` に同梱される `data.json` の `version` に従います。将来 `mapping.json` 側に互換性を壊す変更が必要になった場合は、`requiredExtensions[]` の仕組み（v0.4.0 §2）を共有します。

`data.json` の `version` を解釈の分岐に使ってはならない（B1）という規則は `mapping.json` にも及びます。構造の判定はフィールドの有無で行ってください。

**`avatarId` は識別用のラベルであり、解決には使いません**（L2）。実装は表示や管理の都合で用いてよく、無視してもかまいません。

---

## 11. 適合実装のチェックリスト

v0.4.0 のチェックリストに加えて、次を満たさなければなりません。

- [ ] フレーム識別子（`frameName` があればそれ、なければ `textureID`）で参照を解決する（1 章）
- [ ] `switch` パーツで、選択されたフレーム識別子を持つ**すべての**レイヤーを表示する（2.2）
- [ ] `requiredExtensions` に `EMG_frame_name` があり、それを理解しない場合は読み込みを拒否する（2.6）
- [ ] `defaultVisible: false` の `static` パーツを初期状態で非表示にする（4.2）
- [ ] 非表示のパーツを対象とする sprite を発火させない（4.5）
- [ ] `presets` の適用が、指定されていない `partID` の状態を変更しない（5.2）
- [ ] `sequence.keys` を `key.t <= t` の規則で解決する（6.2）
- [ ] `tracks` を 7.4 の順序で適用する（実装する場合）
- [ ] `tracks` を実装しない場合、当該 sprite を無視し、エラーとしない（8.3）

---

## 12. バージョン履歴

| バージョン | 変更内容 |
|---|---|
| 0.3.0 | `mapping.json` の追加。2026-08-23 改訂で記載漏れを明文化 |
| 0.4.0 (Draft) | 互換性の規則、`requiredExtensions[]`、`layers[].anchor_*`、`fps` の任意化、`blendMode` とテクスチャの確定。表現力の追加なし |
| 0.5.0 (Draft) | フレーム識別子の概念を導入。`frameName` / `control` / `defaultVisible` / `presets[]` / `sequence.keys[]` / `sprites[].tracks[]` を追加。`targetPartID` の `switch` 限定を `sequence` 側へ付け替え。**破壊的変更なし** |
| **0.5.3 (Draft)** | `sprites[].targetLayer` を追加（7.4.1）。トランスフォームの対象をパーツ内の 1 レイヤー（フレーム識別子）に絞れるようにした。宣言識別子は `EMG_layer_transform`。あわせて 7.4 の適用順序で使うアンカーが**レイヤーごとに独立している**ことを明文化し、sprite の競合が未定義になる条件を「同一パーツ」から「同一レイヤー」へ絞った。**撤回した規定は無い**。2026-08-27 |
| **0.5.2 (Draft)** | 実装間で結果を一致させるための規定を追加（10 章）。座標軸の向き、sRGB のまま合成すること、straight アルファ、`textureZIndex` 同値時は宣言順、`sequence` と `tracks` の併用と `loop` の適用範囲、`fps` と `keys` の優先順位、sprite 競合の解決、参照欠落と異常系の扱い。**表現力の追加は無く、これまで実装依存だったものを確定させたもの**。2026-08-26 |
| **0.5.1 (Draft)** | `defaultVisible` の適用範囲を `switch` へ拡大（§4.3）。**0.5.0 の「`switch` では無視せよ」を撤回**。`switch` で使う場合は `EMG_switch_none` の宣言を要求する。2026-08-25 |
