# EMG v0.3.0 → v0.4.0 比較

**更新日:** 2026-08-24
**対象:** `emg-json-spec.md`（v0.3.0）と `emg-json-spec-0.4.0.md`（v0.4.0 Draft）

---

## 1. 一言でいうと

**v0.4.0 は表現力を一切増やしません。** 描画結果でできることは v0.3.0 と同じです。

増えるのは「**この先フィールドを足しても壊れない**」という保証だけです。v0.3.0 には、未知のフィールドや未知の値に出会った実装がどう振る舞うべきかの規定が**一切ありませんでした**。その状態では、どんな追加も既存実装に対して未定義動作になります。

---

## 2. ファイルの互換性

| 方向 | 結果 |
|---|---|
| v0.3.0 のファイルを v0.4.0 実装で読む | **そのまま読めます。** 追加フィールドはすべて任意で、不在時の意味が v0.3.0 の挙動と一致します |
| v0.4.0 のファイルを v0.3.0 実装で読む | **`requiredExtensions` を使わない限り読めます。** v0.4.0 は識別子を 1 つも定義していないため、実際には常に読めます |

**破壊的変更はありません。** 既存ファイルの再書き出しは不要です。

---

## 3. スキーマの差分

### 3.1 追加されたフィールド

| フィールド | 位置 | 不在時 | v0.4.0 での効果 |
|---|---|---|---|
| `requiredExtensions` | ルート | 空配列 | 理解できない識別子があれば読み込みを拒否させる |
| `anchor_x` / `anchor_y` | `layers[]` | `basePosition_x` / `_y` と同値 | **描画に影響しません**（v0.4.0 に座標変換が無いため） |

### 3.2 変更されたフィールド

| フィールド | v0.3.0 | v0.4.0 |
|---|---|---|
| `sprites[].fps` | **必須** | **任意**。不在時は `12` |

必須 → 任意の緩和なので、既存ファイルは影響を受けません。

### 3.3 確定した規定（フィールドは増えません）

| 事項 | v0.3.0 | v0.4.0 |
|---|---|---|
| `blendMode` の値 | 未規定 | CSS `mix-blend-mode` と同一の集合。未知の値は `normal` として扱う |
| ビット深度 | 未規定 | チャンネルあたり 8bit |
| 色空間 | 未規定 | sRGB。ICC は解釈しなくてよい |

---

## 4. 最大の差分：互換性の規則

v0.3.0 に相当する規定は**存在しませんでした**。v0.4.0 が新設したのは次の 5 つです。

| # | 規則 | 意味 |
|---|---|---|
| **F1** | 未知のキー・フィールドは無視して読み進める | 将来の追加でエラーにしない |
| **F2** | 未知の `parts[].type` は、`default` を持つなら `switch`、持たないなら `static` | 未知の種別でも絵が成立する |
| **F3** | 未知の `sequence.type` は `ordered` | |
| **F4** | 未知の `trigger.type` は自律発火しない | 得体の知れないトリガーで動き出すより静止するほうが安全 |
| **F5** | `requiredExtensions` に未知の識別子があれば読み込みを拒否 | 黙って誤った絵を描くより明示的に失敗させる |

### 4.1 F2 がなぜ重要だったか

**規定が無かった状態では、実装が 2 系統に割れていました。** 実測で確認しています。

| 系統 | 実装 | 未知の `type` に対する挙動 |
|---|---|---|
| static 扱い | `emg-cdn` / `emg-web-runtime` / Unity / Godot | **全レイヤーを表示** |
| パーツ消失 | `Emg.Core` / `emg-renpy` | **何も描画しない** |

同じファイルが、実装によって「全部重なる」か「消える」かに分かれていたことになります。

実測した具体的な数字です（基準は 18 枚）。

- **リファレンスプレイヤー**: `Mouth` の `type` を未知の値にすると **23 枚**。口の差分 6 枚が顔の上に全部重なりました
- **`Emg.Core`**: `Character` で 18 → **17 枚**、`arms` で 18 → **15 枚**。パーツが消えました

v0.4.0 適用後は、どちらも 18 枚で基準と一致します。

### 4.2 F2 の判定方法を「`default` の有無」にした理由

多数派は「一律 static 扱い」でしたが、それを採りませんでした。**多数派の挙動こそが「口が 6 枚重なる」絵を生んでいた**ためです。

`default` を見て `switch` と判定すれば 1 枚に収まります。6 実装はいずれも外部へ広く配布されておらず、合わせるべき既存ベースが存在しないため、劣化の質を優先しました。

---

## 5. 実装への影響

### 5.1 対応状況

**6 実装すべてが v0.4.0 に適合済みです。**

| 実装 | 検証方法 |
|---|---|
| `emg-ymm4`（`Emg.Core`） | **実測**（10 ケース全通過） |
| `emg-cdn` プレイヤー | **実測**（A〜G 一致 / H 拒否） |
| `emg-renpy` | **実測**（A〜G 一致 / H 拒否） |
| `emg-godot` | コード読解のみ |
| `emg-unity-importer` | 構文検査のみ（Roslyn） |
| `emg-web-runtime` | 型検査のみ（`tsc`） |

**後半 3 つは実行環境が無く、実機確認ができていません。**

### 5.2 実装側の典型的な変更

いずれの実装も、規則を**1 箇所に集約**する形にしています。分岐箇所に直接書くと、将来の呼び出し側が生のフィールドを読んで同じ不具合を再現するためです。

| 実装 | 集約先 |
|---|---|
| `Emg.Core` | `EmgPart.ResolvedType` 等のプロパティ、`EmgCompat.Validate` |
| `emg-cdn` プレイヤー | `resolvePartType` 等の関数 |
| `emg-godot` / `emg-renpy` | `_resolve_part_type` 等のヘルパ |
| `emg-unity-importer` | `EmgPart.ResolvedType` |
| `emg-web-runtime` | `src/core/EmgCompat.ts` |

### 5.3 書き出し側

**変更は不要です。** v0.4.0 の追加フィールドはいずれも任意で、既存の出力がそのまま有効です。

`anchor_*` を書き出す必要もありません（不在時の既定値が `basePosition` と同値のため）。

---

## 6. v0.5.0 との関係

v0.4.0 は v0.5.0 の**前提条件**です。

v0.5.0 は `frameName` を導入しますが、これは「無視すると誤った絵になる」唯一の機能であり、`requiredExtensions` による宣言を必要とします。その仕組みを提供するのが v0.4.0 の 2 章です。

同様に、`sequence.keys[]`（不等間隔）は `fps` の任意化を、`tracks[]`（トランスフォーム）は `anchor_*` を前提とします。

### 6.1 導入の順序が重要な理由

**前方互換の規定は、対応した実装が配布される前にしか導入できません。**

規定を後から足しても、すでに世に出た実装の振る舞いは変わりません。したがって v0.4.0 は、実装が広く配布される前に完了している必要があります。

現時点で外部配布に最も近いのは `emg-ymm4` の `.ymme` パッケージです。

---

## 7. 関連文書

| 文書 | 内容 |
|---|---|
| `emg-json-spec.md` | v0.3.0（基準となる完全な仕様） |
| `emg-json-spec-0.4.0.md` | v0.4.0 差分仕様。§9.2 に実測結果 |
| `emg-json-spec-0.5.0.md` | v0.5.0 差分仕様（Draft） |
| `emg-extensions-registry.md` | `requiredExtensions` の機能識別子 登録簿 |
| `tools/make-compat-fixtures.js` | §9.2 の検証フィクスチャ生成 |
