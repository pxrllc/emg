# EMG (easy Movable Graphic) 仕様意図・コンセプト

本書は、EMGフォーマット（v0.2.2）がどのような意図で設計されているか、その背景にある「設計思想」のみを抽出したものです。

## 1. 全体的な目的
**「パーツ分割された2D素材を、軽量かつ汎用的に『動かせる状態』で配布・利用すること」**

動画編集ソフトやゲームエンジン（Unity, Web等）など、特定のツールに依存せず、どこでも同じように表示・制御できる共通フォーマットを目指しています。PSDファイルのような「編集用データ」と、ゲームで使う「実行用データ」の中間にある、**「汎用的な再生用フォーマット」**という立ち位置です。

### 1.1. キャラクター専用ではない

`data.json` に「人物」を前提とした構造はどこにもありません。あるのは
**「重ねて描くレイヤー（`static`）」と「差し替えるコマ（`switch`）」**だけで、
背景、小物、UI パーツ、シーンの切り替え、エフェクトのコマ送りも同じ仕組みで表せます。

キャラクターは最も作り込まれた用途というだけで、唯一の用途ではありません。
まばたき・口パク・表情という**意味**を与えるのは任意のコンパニオンファイル
`mapping.json` の側で、そこだけが人物を前提にしています。この分離自体が意図的なもので、
本体のスキーマを用途から独立させておくためのものです。

以下の設計意図で「キャラクター」を例に出している箇所は、あくまで例です。

## 2. 主な設計意図

### 2.1. ランタイムパフォーマンスへの配慮
- **テクスチャアトラスの採用**
  - バラバラのパーツ画像を1枚（または数枚）の「アトラス画像」にまとめることで、描画時の負荷（Draw Call）を最小限に抑えることを意図しています。
- **メタデータの事前提供**
  - バージョン0.2.2で追加された `textures` 配列により、プログラムは**画像ファイル自体を読み込む前に画像のサイズ情報を知ることができます**。これにより、非同期読み込みやUV計算の初期化を高速かつスムーズに行うことを意図しています。

### 2.2. 「制作」と「制御」の分離
- **手動アサインによる `partID`**
  - PSDのレイヤー名（制作側の都合）と、プログラムから制御する時のID（制御側の都合）を切り離しています。
  - イラストレーターがレイヤー名を「右目_笑い_修正2」のように自由につけても、パッキング時に `eye` というIDに紐づけることで、プログラム側は常に `eye` として安定して制御できます。素材の更新によるプログラム修正の手間をなくす意図があります。

### 2.3. 「静」と「動」の明確な構造化
- **`static` と `switch` の分離**
  - 「重ねて描く土台（体・背景・部屋の壁）」と「1 つだけ選んで描くパーツ（目・口・看板の表示内容・エフェクトのコマ）」をデータ構造レベルで明確に区別しています。
  - これにより、ランタイム（再生プレイヤー）の実装がシンプルになり、「どのパーツが切り替わる可能性があるか」を解析する必要がなくなります。

### 2.4. 自律動作と外部制御の共存
- **`trigger` フィールドによる振る舞いの定義**
  - アニメーションデータ（`Sprite`）の中に「いつ再生するか（`trigger`）」の情報を含めることで、**単純な繰り返し（瞬き、待機モーション、ネオンの点滅、水面のゆらぎ）をプレイヤー単体で完結させる**ことを意図しています。これにより、利用側のコードを書かずに絵が生きているように見せることができます。
- **外部制御の許容**
  - 一方で、口パクのように「音声に合わせて動かす」ものは外部から制御する必要があります。`trigger` を持たない、あるいは `external` とすることで、これらも同じ仕組みの中で扱えるように設計されています。

### 2.5. 「動き」と「絵柄」の分離
- **Sequence（順序）の抽象化**
  - アニメーション定義を「具体的なテクスチャIDの列」として定義しつつも、再生ロジック（`ordered` や `random_hold`）を型として持たせています。
  - これにより、「決まった順序で動くアニメ（瞬き）」と「ランダムに切り替わる表情（待機中のバリエーション）」を統一的なデータ構造で表現する意図があります。
